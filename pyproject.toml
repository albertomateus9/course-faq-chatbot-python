[project]
name = "course-faq-chatbot-python"
version = "0.1.0"
description = "Rule-based terminal chatbot that answers synthetic questions about a technical course using editable JSON."
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Alberto Mateus P. da Gama" }]

[tool.pytest.ini_options]
pythonpath = ["."]
